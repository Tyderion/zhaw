package exercises2;

public class Notes {

//        if (stack.length == sp) {
//            // Grow stack
//            double[] bigger = new double[stack.length * 2];
//            System.arraycopy(stack, 0, bigger, 0, stack.length);
//            stack = bigger;
//        }
}
